@model AspCoreFirstApp.Models.Genre

@{
    ViewData["Title"] = "Créer un Genre";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow">
                <div class="card-header bg-success text-white">
                    <h3 class="mb-0">
                        <i class="bi bi-collection"></i> @ViewData["Title"]
                    </h3>
                </div>
                <div class="card-body">
                    <form method="post" novalidate class="needs-validation">
                        @if (ViewData.ModelState.ErrorCount > 0)
                        {
                            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                                <i class="bi bi-exclamation-circle"></i> <strong>Erreur!</strong> Veuillez corriger les erreurs ci-dessous.
                                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                            </div>
                        }

                        <div class="mb-3">
                            <label for="Name" class="form-label">
                                <i class="bi bi-tag"></i> Nom du Genre <span class="text-danger">*</span>
                            </label>
                            <input type="text" 
                                   class="form-control @(ViewData.ModelState.ContainsKey("Name") && ViewData.ModelState["Name"].Errors.Count > 0 ? "is-invalid" : "")" 
                                   id="Name" 
                                   name="Name" 
                                   placeholder="Ex: Action, Comédie, Drame, Horreur..."
                                   value="@Model?.Name"
                                   required
                                   minlength="2"
                                   maxlength="100">
                            @if (ViewData.ModelState.ContainsKey("Name") && ViewData.ModelState["Name"].Errors.Count > 0)
                            {
                                <div class="invalid-feedback d-block">
                                    @ViewData.ModelState["Name"].Errors[0].ErrorMessage
                                </div>
                            }
                            <small class="form-text text-muted">
                                Entre 2 et 100 caractères. Le nom du genre est obligatoire.
                            </small>
                        </div>

                        <div class="form-text mb-4 p-3 bg-light rounded">
                            <i class="bi bi-info-circle"></i> <strong>Remarque :</strong> Les champs marqués avec un astérisque (<span class="text-danger">*</span>) sont obligatoires.
                        </div>

                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-success">
                                <i class="bi bi-check-circle"></i> Créer le Genre
                            </button>
                            <a href="@Url.Action("Index", "Genre")" class="btn btn-secondary">
                                <i class="bi bi-arrow-left"></i> Retour
                            </a>
                        </div>
                    </form>
                </div>
            </div>

            <!-- Genres Populaires Section -->
            <div class="card mt-4 border-success">
                <div class="card-header bg-success text-white">
                    <i class="bi bi-star"></i> Genres Populaires
                </div>
                <div class="card-body">
                    <div class="row g-2">
                        <div class="col-6">
                            <span class="badge bg-primary">Action</span>
                        </div>
                        <div class="col-6">
                            <span class="badge bg-danger">Horreur</span>
                        </div>
                        <div class="col-6">
                            <span class="badge bg-warning text-dark">Comédie</span>
                        </div>
                        <div class="col-6">
                            <span class="badge bg-info">Drame</span>
                        </div>
                        <div class="col-6">
                            <span class="badge bg-secondary">Thriller</span>
                        </div>
                        <div class="col-6">
                            <span class="badge bg-dark">Science-Fiction</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .card {
        border: none;
        border-radius: 10px;
    }

    .card-header {
        border-radius: 10px 10px 0 0;
        padding: 1.5rem;
    }

    .form-label {
        font-weight: 600;
        color: #333;
        margin-bottom: 0.5rem;
    }

    .form-control:focus {
        border-color: #198754;
        box-shadow: 0 0 0 0.25rem rgba(25, 135, 84, 0.25);
    }

    .btn {
        padding: 0.5rem 1.5rem;
        font-weight: 500;
        border-radius: 5px;
        transition: all 0.3s ease;
    }

    .btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.15);
    }

    .text-danger {
        font-weight: bold;
    }

    .badge {
        padding: 0.5rem 1rem;
        font-size: 0.875rem;
    }
</style>

@section Scripts {
    <script>
        // Validation côté client Bootstrap
        (function () {
            'use strict';
            window.addEventListener('load', function () {
                var forms = document.querySelectorAll('.needs-validation');
                Array.prototype.slice.call(forms).forEach(function (form) {
                    form.addEventListener('submit', function (event) {
                        if (!form.checkValidity()) {
                            event.preventDefault();
                            event.stopPropagation();
                        }
                        form.classList.add('was-validated');
                    }, false);
                });
            });
        })();
    </script>
}
